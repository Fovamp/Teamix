# Teamix Cloud 框架配置
teamix:
  name: "Teamix Cloud"
  default_model: "deepseek-v3"

# 以下为可选字段，不写则使用默认文件名
# projects_file: "projects.yaml"
# users_file: "users.yaml"
# mcp_file: "mcp.yaml"

# ---- 以下为 legacy 字段 (向后兼容) ----
project:
  name: "Teamix Cloud"
workflow:
  auto_advance: true
